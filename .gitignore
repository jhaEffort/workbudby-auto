# 推送到 GitHub 前，把下面这些文件挡在外面
# 这些都属于「你自己的私密信息」，一旦提交就是公开泄露

# 账号 token，绝不能提交
tokens.txt

# 客户端登录态文件
*.info

# 推送配置（含 Server酱 sendkey 等密钥）
# 注意：注释必须独占一行，写成 `tokens.txt  # 注释` 会让整行变成匹配模式导致规则失效
.notify.json

# 隐私钩子用的本机账号尾号（pre-commit 也会拦，双保险）
.githooks/privacy-tails.local

# 登录态缓存 / 数据库快照 / 运行日志
auth_cache/
backups/
logs/
run.log
daily.log
daily_bonus.log

# Python
__pycache__/
*.db
*.db-wal
*.db-shm

# macOS
.DS_Store
